---
description: Scaffold a local-first feature in the current workspace and keep the result aligned with existing project patterns.
---

Scaffold the requested feature in the current workspace.

Requirements:
- Stay local-first and prefer existing repository patterns over new abstractions.
- Briefly outline the file plan before proposing edits or code.
- If the selected chat participant supports workspace tools, inspect the relevant files before changing them.
- Keep the implementation concrete: file paths, code blocks, and a clear execution order.
- Include focused tests when the scaffold changes behavior or introduces new logic.
